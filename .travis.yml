before_script:
    - echo "extension=ldap.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
    - composer self-update
    - composer install
language: php
branches:
  only:
  - master
php:
    - 7.1
script:
    - ./vendor/bin/phpunit
notifications:
    email: false
